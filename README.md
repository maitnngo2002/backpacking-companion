# A website that recommends you hotels, restaurants, attractions, etc. every place on Earth

Created with React, Google Map API, RapidAPI

# To run the web app : clone it -> run `npm install` and then run `npm start`
